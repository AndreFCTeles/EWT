{
   "$schema": "../gen/schemas/desktop-schema.json",
   "identifier": "fs",
   "windows": ["main"],
   "permissions": [
      "fs:default",
      { 
         "identifier": "fs:allow-exists", 
         "allow": [{ "path": "$APPDATA/*" }] 
      }
   ]
}